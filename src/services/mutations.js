export default{
    setSpinner(state, value){
        state.spinner = value;
    },
    changeMenu(state, menu){
        state.menu = menu;
    },
    setPosts(state, posts){
        state.posts = posts;
    }
}