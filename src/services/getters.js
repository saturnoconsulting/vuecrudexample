export default {
    getSpinner: state => {
        return state.spinner;
    },
    getStatusMenu: state => {
        return state.menu;
    },
    getPosts: state => {
        return state.posts;
    }
}